'use strict';

angular.module('sparkonixWebApp').controller('addcustomer1Controller',
		addcustomer1Controller);

function addcustomer1Controller($scope, $state, restAPIService, dialogs,
		$rootScope, parseExcelService) {
	console.log("Testing here-------");
	alert("Click on add reseller");
}